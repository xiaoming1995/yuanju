## ADDED Requirements

### Requirement: Early-spring thermal regulation requires fire
The system SHALL classify a natal chart whose month branch is `寅` as `初春偏寒` for cold/heat thermal regulation and SHALL include `火` in its required thermal elements. This thermal result SHALL remain separate from the day-stem regulation result.

#### Scenario: 寅月 chart contains visible fire
- **WHEN** a chart has month branch `寅` and a visible heavenly stem of `丙` or `丁`
- **THEN** its thermal assessment SHALL require `火` and report the fire requirement as satisfied through visible evidence

#### Scenario: 寅月 chart lacks fire evidence
- **WHEN** a chart has month branch `寅` and has no visible or hidden fire evidence
- **THEN** its thermal assessment SHALL require `火` and report the requirement as unresolved

### Requirement: Month command participates in 扶抑 strength
The global 扶抑 assessment SHALL include a separately recorded month-command relationship between the day-master element and the month-branch element before deriving strength level and 扶抑 yongshen.

#### Scenario: Day master meets its month command
- **WHEN** the day-master element equals the month-command element
- **THEN** the assessment SHALL record a positive `月令得令` evidence item and SHALL include that contribution in the global strength total

#### Scenario: Day master is generated by month command
- **WHEN** the month-command element generates the day-master element
- **THEN** the assessment SHALL record a positive month-command support evidence item with less force than `月令得令`

#### Scenario: Month command drains or controls day master
- **WHEN** the day-master and month-command elements have a draining or controlling relationship
- **THEN** the assessment SHALL record the corresponding directional month-command evidence and SHALL include it in the global strength total

### Requirement: Shared-priority yongshen is derived without merging layers
The natal assessment SHALL expose a shared-priority yongshen alignment containing every element that is both required by cold/heat thermal regulation and selected as a 扶抑 yongshen. It SHALL retain the complete, independent results of thermal regulation, day-stem regulation, and 扶抑 assessment.

#### Scenario: Fire is needed by thermal regulation and 扶抑
- **WHEN** thermal regulation requires `火` and the 扶抑 yongshen list includes `火`
- **THEN** the alignment SHALL contain `火` and identify `寒热调候` and `扶抑` as its sources

#### Scenario: Only one layer selects an element
- **WHEN** an element appears in thermal regulation or 扶抑 yongshen but not both
- **THEN** the alignment SHALL NOT mark that element as a shared-priority yongshen

#### Scenario: Target early-spring chart is recalculated
- **WHEN** `丙子·庚寅·乙亥·丙戌` is assessed
- **THEN** thermal regulation SHALL require and satisfy `火`, the 扶抑 result SHALL not remain unresolved through score cancellation, and the alignment SHALL expose `火` as shared priority

### Requirement: Shared priority does not add a duplicate natal-grade score
The shared-priority yongshen alignment SHALL be explanatory only and SHALL NOT independently alter the natal score, grade, grade cap, or vehicle tier.

#### Scenario: Alignment is present
- **WHEN** one or more shared-priority yongshen elements are derived
- **THEN** their display evidence SHALL have no additional scoring delta beyond the already calculated thermal and 扶抑 assessments
