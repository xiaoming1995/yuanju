## 1. 后端：AI 报告 Prompt 对齐系统定格

- [x] 1.1 在 `backend/internal/service/report_service.go` 的 `buildBaziPrompt()` 中新增 `[系统定格结果]` 区块，注入 `MingGe` / `MingGeDesc`
- [x] 1.2 为系统定格区块增加硬约束：输出中凡涉及格局主名，必须与系统主格一致，不得重新改判
- [x] 1.3 为旧 `result_json` 无 `MingGe` 的场景设计降级路径，避免旧命盘生成报告时报错

## 2. 后端：重写格局相关 Prompt 语义

- [x] 2.1 将第一步中的格局模块从“格局评分/重新定格”改为“格局解释/以系统主格为准”
- [x] 2.2 在格局模块中补充“成格 / 破格 / 有救 / 偏弱 / 兼象”的解释边界
- [x] 2.3 重写第二步 `analysis.logic` 要求，使其固定按“主格 → 格局状态 → 调候与喜忌关系 → 亮点与缺陷”的顺序展开
- [x] 2.4 增加输出一致性约束：若存在其它结构，只能写为“兼带某某倾向”或“局中亦见某某气象”，不得覆盖主格

## 3. 知识库：`kb_gejv` 改为解释型短版

- [x] 3.1 将默认 `kb_gejv` 内容从“重新定格规则”改为“解释系统主格”的短版知识库
- [x] 3.2 在新 `kb_gejv` 中明确格局状态判断口径：成格、破格、有救、偏弱、兼象
- [x] 3.3 在新 `kb_gejv` 中明确格局与最终喜用神的关系：格局喜神不等于最终唯一用神，调候可优先
- [x] 3.4 处理 Admin 已自定义 `kb_gejv` 的场景，明确本轮是否仅更新默认 seed、不强制覆盖现网自定义值

## 4. 验证

- [x] 4.1 生成一份带 `MingGe` 的新报告，确认 `analysis.logic` 首句明确写出系统主格
- [x] 4.2 验证顶部命格 Badge 与 `analysis.logic` 中的格局主名一致
- [x] 4.3 验证格局兼象场景下，AI 使用“兼带某某倾向”表达，而非覆盖主格
- [x] 4.4 验证调候与格局方向不一致时，AI 明确说明“格可论，但调候更为关键”或同义表达
- [x] 4.5 抽查一条旧快照（无 `MingGe`）命盘，确认降级路径可正常生成报告、不报错
