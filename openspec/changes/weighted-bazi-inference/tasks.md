## 1. 改写 CoT 第一步推断指令

- [x] 1.1 将 `report_service.go` 中 `buildBaziPrompt()` 第一步 CoT 的 a/b/c 三段改写为四段：调候评分（65票）、格局评分（25票）、神煞评分（10票）、加权合并
- [x] 1.2 调候模块（a）：要求 AI 基于精算注入的调候数据和大运征兆，对每个相关五行/天干输出 `喜+65` 或 `忌-65` 的票数
- [x] 1.3 格局模块（b）：要求 AI 按知识库 `【格局判断规则】` 定格后，对格局喜用神输出 `+25`，忌神输出 `-25`
- [x] 1.4 神煞模块（c）：要求 AI 扫描全部神煞对五行的影响方向，输出 `+10` 或 `-10` 或 `0`
- [x] 1.5 加权合并（d）：要求 AI 对每个五行汇总票数，正者为喜用神、负者为忌神；若模块矛盾标注「调候优先，格局/神煞结论有出入」

## 2. 改写第二步报告输出要求

- [x] 2.1 在 `analysis.logic` 生成要求中新增：首段必须包含加权推断说明（权重比例 + 最终用神 + 若有矛盾注明哪个模块被覆盖）
- [x] 2.2 调整「开门见山说格局定性」的格式要求，改为「开门见山说加权推断结论（如：本命以调候为主导，最终用神为丙火，格局建议壬水，按权重调候优先）」

## 3. 验证

- [x] 3.1 执行 `go build ./...` 编译验证无语法错误
- [ ] 3.2 重启后端服务（使调候数据库迁移同步生效）
- [ ] 3.3 手动生成一份八字报告，检查 `analysis.logic` 首段是否包含权重推断说明
- [ ] 3.4 验证矛盾场景：选一个调候与格局用神不同的典型命盘，确认报告中注明了矛盾来源
