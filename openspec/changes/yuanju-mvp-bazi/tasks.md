## 1. 项目基础搭建

- [x] 1.1 初始化 Go 后端项目结构（cmd/api、internal/、pkg/、configs/）
- [x] 1.2 初始化 React 前端项目（Vite + TypeScript）
- [x] 1.3 配置 Docker Compose（PostgreSQL + Redis + 后端 + 前端）
- [x] 1.4 编写 .env 配置模板（数据库、AI API 密钥、JWT 密钥等）
- [x] 1.5 设计并执行数据库初始化迁移（users、bazi_charts、ai_reports 表）

## 2. 用户认证模块（后端）

- [x] 2.1 实现用户注册接口 `POST /api/auth/register`（bcrypt 加密、邮箱唯一性校验）
- [x] 2.2 实现用户登录接口 `POST /api/auth/login`（验证密码、颁发 JWT）
- [x] 2.3 实现获取当前用户接口 `GET /api/auth/me`
- [x] 2.4 编写 JWT 鉴权中间件，保护需登录的接口

## 3. 八字算法引擎（后端）

- [x] 3.1 实现阳历 → 农历转换算法（基于寿星万年历数据）
- [x] 3.2 实现年柱天干地支推算（基于农历年干支）
- [x] 3.3 实现月柱天干地支推算（基于节气分界）
- [x] 3.4 实现日柱天干地支推算（万年历日柱查表）
- [x] 3.5 实现时柱天干地支推算（含早子时/晚子时边界处理）
- [x] 3.6 实现五行分布统计（统计十天干十二地支的五行属性与比例）
- [x] 3.7 实现用神喜忌推算（基于日主强弱判断）
- [x] 3.8 实现大运序列推算（起运年龄 + 10步大运干支 + 年份范围）
- [x] 3.9 编写八字计算单元测试（用知名人物八字作基准验证）
- [x] 3.10 实现八字计算接口 `POST /api/bazi/calculate`

## 4. AI 报告模块（后端）

- [x] 4.1 封装大模型 API 客户端（DeepSeek 主力 + OpenAI 备选，统一接口）
- [x] 4.2 设计八字报告 Prompt 模板（四章节：性格/感情/事业/健康）
- [x] 4.3 实现 AI 报告生成接口 `POST /api/bazi/report`（含30秒超时处理）
- [x] 4.4 实现报告缓存逻辑（相同命盘 hash 命中缓存，不重复调用 API）

## 5. 历史记录模块（后端）

- [x] 5.1 实现历史记录保存逻辑（AI 报告生成成功后自动入库）
- [x] 5.2 实现历史记录列表接口 `GET /api/bazi/history`（按时间倒序，分页）
- [x] 5.3 实现历史记录详情接口 `GET /api/bazi/history/:id`（含越权访问拦截）

## 6. 前端设计系统搭建

- [x] 6.1 设计全局 CSS 变量（颜色系统：金木水火土配色 + 深色主题基础色）
- [x] 6.2 配置 Google Fonts（Inter 或 Noto Sans SC）
- [x] 6.3 封装通用 UI 组件（Button、Input、Card、Modal、Loading）
- [x] 6.4 配置前端路由（React Router：首页/登录/注册/结果/历史）
- [x] 6.5 封装 API 请求层（axios 拦截器 + JWT 自动注入 + 错误统一处理）

## 7. 前端页面开发

- [x] 7.1 开发首页（生辰输入表单：年月日时 + 性别选择，提交按钮）
- [x] 7.2 开发注册/登录页（表单校验 + 错误提示）
- [x] 7.3 开发命盘结果页——简洁视图（AI 解读摘要卡片，默认展示）
- [x] 7.4 开发命盘结果页——专业视图（四柱命盘表格 + 五行属性颜色标注）
- [x] 7.5 开发五行雷达图组件（纯 CSS/Canvas 实现，无需第三方图表库）
- [x] 7.6 开发大运时间轴组件（水平滚动，当前大运高亮）
- [x] 7.7 开发历史记录列表页（卡片列表 + 空状态引导）
- [x] 7.8 连接后端 API（首页计算、AI 报告生成、历史记录）

## 8. 体验优化与收尾

- [x] 8.1 添加 AI 报告生成时的 Loading 动画（骨架屏或流光效果）
- [x] 8.2 实现响应式布局（移动端适配，命盘纵向排列）
- [x] 8.3 在 AI 报告末尾添加免责声明文字
- [x] 8.4 实现未登录用户的引导弹窗（提示登录以保存记录）
- [x] 8.5 配置生产环境 Docker 部署脚本
- [x] 8.6 端对端功能验收测试（完整流程：输入生辰→计算→AI报告→保存→历史查看）
